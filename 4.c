// Area of square

# include<stdio.h>

int main(){
    float side;
    printf("Enter the side of square: ");
    scanf("%f", &side);

    printf("Required area of square is: %f", side*side);
    return 0;
}